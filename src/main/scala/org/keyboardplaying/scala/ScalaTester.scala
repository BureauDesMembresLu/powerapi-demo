package org.keyboardplaying.scala

/**
  * Just a basic test to ensure Scala is running properly.
  *
  * @author Cyrille Chopelet
  */
class ScalaTester {
  def runScala(name: String) = {
    println("Hello, " + name + "!")
  }
}
